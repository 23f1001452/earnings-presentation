# Quarterly Earnings — RevealJS Presentation

This repository contains a RevealJS presentation for the Q3 2025 earnings report.

**Presenter:** 23f1001452@ds.study.iitm.ac.in

## Publish

This repository uses a GitHub Actions workflow to publish to `gh-pages` branch. Push to `main` to trigger a deploy.

## URL

After deployment, the site will be available at:

`https://<GITHUB_USERNAME>.github.io/<REPO_NAME>/`

(If you use `earnings-presentation` as repo name, replace `<REPO_NAME>` with `earnings-presentation`.)
